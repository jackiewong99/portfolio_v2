import React from 'react';
// Styling
import styles from './Projects.module.css';

const ProjectCard = () => {
  return (
    <div>
      <header>
        <h1 className={styles.cardHeader}>Projects</h1>
      </header>
    </div>
  );
};

export default ProjectCard;
